Install pipenv (if needed):
    pip install pipenv

Activate virtual environment:
    cd swift_connect
    pipenv shell

Install dependencies:
    pipenv install

Run the application:
    flask run